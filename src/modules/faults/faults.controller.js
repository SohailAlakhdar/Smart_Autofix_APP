/**
 *
 * POST   /api/faults/diagnose         // body: {text} or multipart {image}
GET    /api/faults/:id
GET    /api/faults/history          // user's past reports
POST   /api/faults/:id/feedback     // rating + helpful?
 */